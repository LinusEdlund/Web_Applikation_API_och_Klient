@model List<NobelPrizeModel>



<div class="container">
    <div class="row p-4 pb-3">
        <div class="col-6" >
            <h2 class="text-primary">Nobel Prize List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Nobel" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-person-plus-fill"></i>Create New Person
            </a>
        </div>
    </div>

    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Year 
                </th>
                <th>

                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model.OrderBy(x => x.Year))
            {
                <tr>
                    <td>@obj.Name</td>
                    <td>@obj.Year</td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Nobel" asp-action="Edit" asp-route-Id="@obj.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i>Edit
                            </a>
                            <a asp-controller="Nobel" asp-action="Delete" class="btn btn-danger mx-2">
                                <i class="bi bi-person-x-fill"></i>Delete
                            </a>

                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
